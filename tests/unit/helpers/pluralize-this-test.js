import { pluralizeThis } from '../../../helpers/pluralize-this';
import { module, test } from 'qunit';

module('Unit | Helper | pluralize this');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pluralizeThis(42);
  assert.ok(result);
});
