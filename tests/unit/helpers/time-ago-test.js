import {
  timeAgo
} from 'ember-addon-review/helpers/time-ago';

module('TimeAgoHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeAgo(42);
  ok(result);
});
