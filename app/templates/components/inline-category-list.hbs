{{#each categoriesSortedByAddonCount as |category|}}
  <CommaSeparated @list={{categoriesSortedByAddonCount}} @item={{category}} as |value separator|>
    <LinkTo @route="categories.show" @model={{value.slug}}>
      {{#if showWithoutParent}}
        {{value.name}}
      {{else}}
        {{#if value.parent}}
          {{value.parent.name}} >
        {{/if}}
        {{value.name}}
      {{~/if~}}
      {{#if showAddonCount}}
        ({{value.totalAddonCount}})
      {{~/if~}}
      {{separator}}
    </LinkTo>
  </CommaSeparated>
{{/each}}
