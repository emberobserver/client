{{#x-select value=position}}
	{{#x-option value=-1}}At end{{/x-option}}
	{{#each categories as |category|}}
		{{#x-option value=category.position}}Before "{{category.name}}"{{/x-option}}
	{{/each}}
{{/x-select}}
