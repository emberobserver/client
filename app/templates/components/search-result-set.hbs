{{#if results.length}}
  <div class="results {{resultClass}}">
    <h5>
      <a href="#" {{action toggleResultsExpansion}}>
        {{#if resultsCollapsed}}
          {{svg-icon "chevron-right"}}
        {{else}}
          {{svg-icon "expand-more"}}
        {{/if}}
        {{title}} ({{totalCount}})
      </a>
    </h5>
    {{#unless resultsCollapsed}}
      <div class="results-content">
        {{yield}}
        {{#if (less-than results.length totalCount)}}
          {{#if fetchMore.isRunning}}
            <button class="button more-results">Fetching more...</button>
          {{else}}
            <button {{action (perform fetchMore)}} class="button more-results">Show more</button>
          {{/if}}
        {{/if}}
      </div>
    {{/unless}}
  </div>
{{/if}}
