{{#if addon.isWip}}
  <span class="score">
    {{yield "WIP"}}
  </span>
{{else}}
  {{#if hasNumericScore}}
    <span class="score" data-score={{addon.score}}>
      {{yield addon.score}}
    </span>
  {{else}}
    <span class="score">
      {{yield "N/A"}}
    </span>
  {{/if}}
{{/if}}
