<span class="addon-count test-addon-table-count">Displaying {{pluralize addons.length "addon"}}</span>
<table class="addons-table">
  <tbody>
  {{#each addon in addons}}
    {{addon-row addon=addon showAddon="showAddon"}}
  {{/each}}
  </tbody>
</table>
