<div class="ember-version-compatibility test-ember-version-compatibility-section">
  <h6>ember compatibility</h6>
  {{#if this.testResult.succeeded}}
    {{#if this.allTestsPassed}}
      <p>
        <span class="test-ember-version-compatibility-semver-compat">
          All tests pass with Ember {{this.compatibilitySemverString}}
        </span>
        <br>
        <em class="test-ember-version-compatibility-timestamp">
          last ran <RelativeTime @date={{this.testResult.testsRunAt}} />
        </em>
      </p>
      {{#if this.showTable}}
        <a href="#" {{action "toggleShowTable"}} class="info-action test-ember-version-compatibility-show-table">Hide results</a>
        <EmberVersionCompatibilityList @versionCompatibilities={{this.sortedVersionCompatibilities}} />
      {{else}}
        <a href="#" {{action "toggleShowTable"}} class="info-action test-ember-version-compatibility-show-table">Show detailed results</a>
      {{/if}}
    {{else}}
      Tests run against Ember {{this.compatibilitySemverString}} but not all tests passed.
      <br>
      <em class="test-ember-version-compatibility-timestamp">
        last ran <RelativeTime @date={{this.testResult.testsRunAt}} />
      </em>
      <EmberVersionCompatibilityList @versionCompatibilities={{this.sortedVersionCompatibilities}} />
    {{/if}}
  {{else}}
    <p>
      <span class="test-ember-version-compatibility-unknown">
        We were unable to run this addon's tests.
      </span>
      <br>
      <em class="test-ember-version-compatibility-timestamp">
        last tried <RelativeTime @date={{this.testResult.testsRunAt}} />
      </em>
    </p>
  {{/if}}
</div>
